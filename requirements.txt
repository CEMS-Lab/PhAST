# PhAST core runtime dependencies.
#
# Prefer `pip install -e .` for normal source installs. This file is kept for
# environments that install dependencies separately from the package metadata.

# Numerics
torch>=2.0.0
numpy>=1.24
scipy>=1.10

# Mesh I/O
meshio>=5.0
gmsh>=4.10

# Visualization
matplotlib>=3.6
Pillow>=9.0

# --- Optional: MP4 animation output ---
# ffmpeg (system package, not pip). Install via:
#   conda install ffmpeg          (recommended)
#   brew install ffmpeg           (macOS)
#   apt install ffmpeg            (Ubuntu/Debian)
# If ffmpeg is not installed, animations fall back to GIF automatically.

# HDF5 output (GNO training data)
h5py>=3.7

# Zarr trajectory stores. Keep pinned to the v2 API until the reader/writer
# stack is migrated to Zarr v3.
zarr>=2.18,<3
numcodecs>=0.13

# YAML configuration decks
pyyaml>=6.0

# --- Optional: AMG preconditioner (pip install phast[amg]) ---
# pyamg>=5.0

# --- Optional: NVIDIA AmgX, CUDA-only (pip install phast[amgx]) ---
# pyamgx>=0.1

# --- Optional: METIS partitioning (pip install phast[metis]) ---
# pymetis>=2020.1
